export enum ColorDestroy {
    Red,
    Green,
    Blue,
    Purple,
    Yellow,
}
export enum LineDestroy {
    Vertical,
    Horizontal,
}
export enum AreaDestroy {
    Default,
    All,
}

export enum TileAbilityTypes {
    ColorDestroy,
    LineDestroy,
    AreaDestroy,
}


